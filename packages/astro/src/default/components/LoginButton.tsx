/**
 * LoginButton is no longer needed with Docker runtime.
 * This is a placeholder component that renders nothing.
 */
export function LoginButton() {
  // docker runtime doesn't require authentication
  return null;
}

